<body class="review-orders">
    <h1>{{name}}'s Orders</h1>
    <h2>{{message}}</h2>
    {{#each orderDetails}}
        <div class="order-block">
            <h2>Order Date: {{this.date}}</h3>
            <h3>Order ID: {{this.orderID}}</h2>
            <a href="/admin/update-order/{{this.id}}">Update Order</a>
            <table class="ordersView-table">
                <thead>
                    <tr>
                        <th>Product Name</th>
                        <th>Quantity</th>
                        <th>Price</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each this.products}}
                        <tr>    
                            <td>{{this.name}}</td>
                            <td>{{this.quantity}}</td>
                            <td>{{this.price}}</td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    {{/each}}
</body>

